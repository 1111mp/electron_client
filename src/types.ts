export enum WindowName {
  Setting = 'Setting',
  Info = 'Info',
}

export enum WindowUrl {
  Setting = '/setting',
  Info = '/info',
}

export enum Theme {
  system = 'system',
  light = 'light',
  dark = 'dark',
}
