import { Component } from 'react';

type Props = {};

export default class Loading extends Component<Props> {
  // props: Props;

  render(): JSX.Element {
    return (
      <div>
        <h2>Loading</h2>
      </div>
    );
  }
}
